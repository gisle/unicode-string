Map8*       T_MAP8

INPUT
T_MAP8
        if (sv_derived_from($arg, \"Unicode::Map8\")) {
            IV tmp = SvIV((SV*)SvRV($arg));
            $var = ($type) tmp;
        }
        else
            croak(\"$var is not an Unicode::Map8\")

OUTPUT
T_MAP8
        sv_setref_pv($arg, \"Unicode::Map8\", (void*)$var);
